---
import { renderFooter, createFooterSettings } from '../../shared/components/footer'

interface Props {
  class?: string
}

const { class: className = '' } = Astro.props
const settings = createFooterSettings({ class: className })
const html = renderFooter(undefined, settings)
---

<div set:html={html} />
